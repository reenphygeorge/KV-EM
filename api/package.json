{
  "name": "api",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "start": "pnpm run build && node dist/app.js",
    "migration:create": "pnpm build && pnpm typeorm migration:create",
    "migration:generate": "pnpm build && pnpm typeorm migration:generate -d dist/db/data-source.db.js",
    "migration:run": "pnpm build && pnpm typeorm migration:run -d dist/db/data-source.db.js",
    "migration:revert": "pnpm build && pnpm typeorm migration:revert -d dist/db/data-source.db.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^0.3.20",
    "typeorm-naming-strategies": "^4.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
